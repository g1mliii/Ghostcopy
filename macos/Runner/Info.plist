<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<key>CFBundleDevelopmentRegion</key>
	<string>$(DEVELOPMENT_LANGUAGE)</string>
	<key>CFBundleExecutable</key>
	<string>$(EXECUTABLE_NAME)</string>
	<key>CFBundleIconFile</key>
	<string></string>
	<key>CFBundleIdentifier</key>
	<string>$(PRODUCT_BUNDLE_IDENTIFIER)</string>
	<key>CFBundleInfoDictionaryVersion</key>
	<string>6.0</string>
	<key>CFBundleName</key>
	<string>$(PRODUCT_NAME)</string>
	<key>CFBundlePackageType</key>
	<string>APPL</string>
	<key>CFBundleShortVersionString</key>
	<string>$(FLUTTER_BUILD_NAME)</string>
	<key>CFBundleVersion</key>
	<string>$(FLUTTER_BUILD_NUMBER)</string>
	<!-- Agent app: no Dock icon, no menu bar. Declared here rather than left
	     to window_manager's skipTaskbar, which is applied once at startup -
	     LaunchServices promoted the app to a regular one when the browser
	     opened ghostcopy:// after OAuth, and nothing put it back. Setting the
	     policy at runtime does not stick while the app is frontmost. -->
	<key>LSUIElement</key>
	<true/>
	<key>LSMinimumSystemVersion</key>
	<string>$(MACOSX_DEPLOYMENT_TARGET)</string>
	<key>NSHumanReadableCopyright</key>
	<string>$(PRODUCT_COPYRIGHT)</string>
	<key>NSMainNibFile</key>
	<string>MainMenu</string>
	<key>NSPrincipalClass</key>
	<string>NSApplication</string>
	<!-- The Finder context-menu entry, alongside the Share extension. Blip and
	     TeamViewer both ship this, and both carry an empty NSRequiredContext -
	     the key is not optional in practice, and its absence is why an
	     otherwise correct entry never appeared. -->
	<key>NSServices</key>
	<array>
		<dict>
			<key>NSMenuItem</key>
			<dict>
				<key>default</key>
				<string>Send with GhostCopy</string>
			</dict>
			<!-- Must match the @objc selector on ShareService. -->
			<key>NSMessage</key>
			<string>sendFileWithGhostCopy</string>
			<key>NSPortName</key>
			<string>GhostCopy</string>
			<key>NSRequiredContext</key>
			<dict/>
			<!-- public.data, not public.item: folders and bundles conform to
			     public.item too, so that advertised the service on directories
			     the send path cannot handle - right-clicking a folder offered
			     the action only to fail with "no longer exists". public.data is
			     the base type for byte streams, which is exactly the regular
			     files this can send. -->
			<key>NSSendFileTypes</key>
			<array>
				<string>public.data</string>
			</array>
		</dict>
	</array>
	<key>CFBundleURLTypes</key>
	<array>
		<dict>
			<key>CFBundleTypeRole</key>
			<string>Editor</string>
			<key>CFBundleURLName</key>
			<string>com.ghostcopy.app</string>
			<key>CFBundleURLSchemes</key>
			<array>
				<string>ghostcopy</string>
			</array>
		</dict>
	</array>
</dict>
</plist>
