<?xml version="1.0" encoding="utf-8"?>
<!--
    Android 12+ (API 31) backup/transfer rules.

    GhostCopy persists the Supabase session and the E2E encryption passphrase
    on device. Neither should leave it: a cloud backup or device-to-device
    transfer would carry the user's account session, and the passphrase is
    deliberately not recoverable from the server. `allowBackup="false"` covers
    API <= 30; these rules cover 31+, where allowBackup alone no longer governs
    device-to-device transfer.
-->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="root" />
        <exclude domain="file" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="root" />
        <exclude domain="file" />
        <exclude domain="database" />
        <exclude domain="sharedpref" />
        <exclude domain="external" />
    </device-transfer>
</data-extraction-rules>
