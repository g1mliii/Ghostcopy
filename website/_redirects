# The two pages folded into the FAQ. 301 so anyone holding an old link, and any
# search engine that indexed one, lands on the replacement rather than on a
# soft 404.
/docs.html      /faq   301
/docs           /faq   301
/support.html   /faq   301
/support        /faq   301

# The current macOS installer. Release assets are versioned and published with
# --latest=false, so GitHub has no stable URL for "the newest DMG". Bump this
# line after each macOS release (docs/macos-releases.md). A stale target is
# harmless: the app updates itself through Sparkle on first launch.
/download/macos https://github.com/g1mliii/Ghostcopy/releases/download/macos-v1.0.0-7/GhostCopy-1.0.0-7.dmg 302
